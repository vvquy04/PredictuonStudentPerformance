{
  "version": 2,
  "builds": [
    {
      "src": "app.py",  // Tên tệp chứa mã nguồn Flask của bạn
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"  // Đường dẫn đến tệp chứa ứng dụng Flask
    }
  ]
}
